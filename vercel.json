{
  "version": 2,
  "installCommand": "npm ci --no-audit --no-fund",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}

